{
  "name": "wallet-mcp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev:cdp": "wrangler dev --port 8788 --inspector-port 9230",
    "build": "wrangler deploy --dry-run",
    "check": "ultracite check src package.json tsconfig.json wrangler.toml README.md",
    "fix": "ultracite fix src package.json tsconfig.json wrangler.toml README.md",
    "types": "bunx wrangler types",
    "typecheck": "bun run types && tsc --noEmit"
  },
  "dependencies": {
    "@coinbase/cdp-sdk": "^1.44.1",
    "viem": "^2.46.3"
  },
  "devDependencies": {
    "typescript": "catalog:workers",
    "wrangler": "^4.66.0"
  }
}
